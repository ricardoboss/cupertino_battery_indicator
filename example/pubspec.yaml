name: example
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.2 <4.0.0'
  flutter: ">=3.24.2"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  cupertino_battery_indicator:
    path: ../

  flutter_lints: ^4.0.0
